buat issue-supplier-restock.md, untuk planing membuat ini:
Tolong buatkan fitur Stok Masuk dari Supplier (Purchase) menggunakan flutter_bloc berdasarkan skema di .agents/rules/supabase_schema.md.

Lakukan urutan berikut:

    Model & Repository: Buat StockMutationModel dan StockMutationRepository. Repository hanya butuh fungsi createPurchaseMutation() untuk melakukan insert ke tabel stock_mutations. Jangan buat fungsi update ke tabel products karena sudah di-handle oleh trigger database.

    State Management: Buat StockMutationCubit (dan state-nya) untuk mengontrol form input.

    UI Form (lib/presentation/inventory/restock_screen.dart):

        Buat dropdown/search untuk memilih Produk (Product).

        Saat produk dipilih, tampilkan field Read-Only berisi harga_modal_terakhir dan current_stock saat ini.

        Buat dropdown untuk memilih Satuan (ProductUnit) dari produk tersebut.

        Buat input qty_original (jumlah barang masuk). Validasi: Harus angka positif.

        Buat input harga_modal_baru (harga beli dari supplier saat ini).

    Logika Kalkulasi Sebelum Insert:

        qty_in_base = qty_original * conversion_to_base dari satuan yang dipilih.

        mutation_type = 'purchase'.

        unit_name_snapshot = unit_name dari satuan yang dipilih.

        harga_modal_lama = harga_modal_terakhir dari produk terpilih.

    Logika UX Pop-up Harga (PENTING):

        Saat tombol Submit ditekan, bandingkan harga. Jika harga_modal_baru > harga_modal_terakhir, jangan langsung insert. Munculkan Dialog Pop-up: "Harga modal naik! Stok akan ditambahkan, namun apakah Anda ingin meninjau ulang Harga Jual sekarang?".

        Jika user klik "Ya", lakukan insert mutasi lalu navigasikan ke halaman edit produk. Jika "Tidak", lakukan insert mutasi dan tutup halaman.

Pastikan desainnya rapi dan error handling (misal form kosong) tertangani."